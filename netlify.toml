[build]
  command = "npm run build"
  publish = ".next"

# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# Set these in Netlify Dashboard → Site configuration → Environment variables
# (Never commit real values)
# ============================================

[build.environment]
  # Email (Resend)
  # RESEND_API_KEY=

  # Telegram alerts
  # TELEGRAM_BOT_TOKEN=
  # TELEGRAM_CHAT_ID=

  # HubSpot CRM
  # HUBSPOT_PRIVATE_APP_TOKEN=

  # OpenPhone SMS
  # OPENPHONE_API_KEY=
  # OPENPHONE_PHONE_NUMBER_ID=   # ID of the phone number to send from (get from OpenPhone)

# Netlify will auto-install the Next.js plugin for full SSR + API support.
# If you want to pin it explicitly, uncomment below:
# [[plugins]]
#   package = "@netlify/plugin-nextjs"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Cache static assets aggressively
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ============================================
# All required environment variables for the lead pipeline
# (Set the actual values in the Netlify dashboard)
# ============================================
# RESEND_API_KEY=
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
# HUBSPOT_PRIVATE_APP_TOKEN=
# OPENPHONE_API_KEY=
# OPENPHONE_PHONE_NUMBER_ID=
